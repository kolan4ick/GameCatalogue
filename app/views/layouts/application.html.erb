<!DOCTYPE html>
<html>
  <head>
    <title>Game Catalogue</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag "will_paginate" %>
  </head>

  <body>
    <header class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <h4>
        <span class="p-2 text-dark"><%= link_to "GC", root_path %></span>
      </h4>
      <span class="p-2 text-dark"><%= link_to "All products", products_path, :class => 'btn btn-warning' %></span>
      <span class="p-2 text-dark"><%= link_to "All categories", categories_path, :class => 'btn btn-warning' %></span>
      <nav class="my-2 my-md-0 me-md-3">
        <%= form_tag search_path, method: :get, class: "navbar-form navbar-left search-form" do %>
        <%= text_field_tag( :search, params[:search], placeholder: "Search", class: "" ) %>
        <%= select_tag "filter", options_for_select(['3', '6', '10', '13', '17', '18'], @filter), onchange:"clickSubmit();", class: 'custom-select', prompt: 'Select age_limit filter' %>
      <% end %>
    </nav>
    <nav class="my-2 my-md-0 me-md-3">
      <div class="row-3">

        <!-- <a class="btn btn-outline-primary" href="#">Sign up</a> -->
      </header>
      <div style="padding: 20px">
        <%= yield %>
      </div>
    </body>
  </html>
